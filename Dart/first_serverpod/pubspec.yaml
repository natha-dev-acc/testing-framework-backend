name: _
publish_to: none

environment:
  sdk: '^3.8.0'

workspace:
  - first_serverpod_client
  - first_serverpod_server
  - first_serverpod_flutter
dependency_overrides:
  flutter_secure_storage: ^10.0.0

